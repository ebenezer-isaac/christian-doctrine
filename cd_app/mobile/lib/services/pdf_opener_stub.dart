import 'dart:typed_data';

/// Non-web stub (mobile would use a file save + open plugin later).
void openPdf(Uint8List bytes, String filename, {bool download = false}) {}

void openUrl(String url) {}
