import 'package:flutter/widgets.dart';

/// Non-web stub. Mobile uses the interactive AuthService.signIn() instead, so
/// this is never shown.
Widget renderGoogleButton() => const SizedBox.shrink();
